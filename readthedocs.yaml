# readthedocs.yaml

version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "mambaforge-4.10"

python:
  install:
    - method: pip
      path: .
    - requirements: docs/pip_requirements.txt

conda:
  environment: docs/requirements.yaml
